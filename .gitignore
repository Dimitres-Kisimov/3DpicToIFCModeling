# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
env/
venv/
.venv/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp files
temp/
tmp/
*.tmp

# OS
.DS_Store
Thumbs.db
.vscode/settings.json

# Build outputs
dist/
build/

# IDE
.idea/
*.swp
*.swo
*~

# Generated files
uploads/
outputs/
data/mesh_library_abo/_listings_work/
backend/trellis/

# SCS demo build artifacts (regenerable)
demo/out/
demo/meshes/

# Large datasets / copies kept out of git
data/office_images/
data/demo_photos/
data/furniture_library/
models/

# Office lock files
~$*

# app build output
demo/app_out/

# paper figure scratch (full scene builds + GLBs)
paper_figures/_work/

# photo->3D eval scratch (rendered photos + recon GLBs)
eval_out/

# bake-off scratch (rendered photos + recon GLBs)
bakeoff_in/
bakeoff_out/

# generated colored catalog previews (regenerate via make_previews.py)
data/mesh_library_abo/*.preview.png

# --- benchmark artifacts (2026-06-30 study): bulky + regenerable ---
# Regenerate meshes/renders via backend/python-scripts/batch_abo_test.py + polyhaven_benchmark.py
# Regenerate galleries/bundles via build_abo_gallery.py / export_abo_gallery.py
outputs/abo_test*/*.glb
outputs/abo_test*/*.png
deliverable/*.zip
deliverable/abo_gallery*/
# keep: outputs/abo_test*/{scores,results}.json, deliverable/*.csv, deliverable/all_scores.json

# cloud benchmark heavy artifacts — keep code + findings in git, not the binaries
deliverable/cloud_results/
deliverable/cloud_gallery/assets/
deliverable/cloud_bundle/
deliverable/cloud_bundle.tar.gz
*.bundle
deliverable_SCS_*.zip
_hf_scout/
.claude/

# local-only session handoff (contains live pod connection — do not push)
deliverable/SESSION_HANDOFF_CLOUD_RUN.md
deliverable/cloud_gallery_export.zip

# building-scale population: keep manifests/tables, ignore regenerable per-room GLBs
deliverable/building/*/rooms/
deliverable/building/*/*.ifc
deliverable/building/*/*.xkt
